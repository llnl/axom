# Using Axom With Python

This example runs against an installed Axom Python package.

From an Axom install prefix, run:

```bash
./bin/run_python_with_axom.sh examples/axom/using-with-python/example.py
```

The helper script sets `PYTHONPATH` for `axom.sidre` and its Python runtime
dependencies.
