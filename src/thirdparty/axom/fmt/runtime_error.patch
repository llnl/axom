diff --git a/include/fmt/format-inl.h b/include/fmt/format-inl.h
index f0b68846..b8c7421f 100644
--- a/include/fmt/format-inl.h
+++ b/include/fmt/format-inl.h
@@ -160,11 +160,17 @@ FMT_API FMT_FUNC auto format_facet<std::locale>::do_put(
 }
 #endif
 
+// BEGIN AXOM BUGFIX
+// NVCC's preprocessor converts 'std::error_code' to `class std::error_code'
+// and then complains that `return class std::error_code' isn't valid!
+using axom_fmt_system_error = std::system_error;
+
 FMT_FUNC auto vsystem_error(int error_code, string_view fmt, format_args args)
-    -> std::system_error {
+    -> axom_fmt_system_error {
   auto ec = std::error_code(error_code, std::generic_category());
-  return std::system_error(ec, vformat(fmt, args));
+  return axom_fmt_system_error(ec, vformat(fmt, args));
 }
+// END AXOM BUGFIX
 
 namespace detail {
 
@@ -1423,16 +1429,18 @@ FMT_FUNC detail::utf8_to_utf16::utf8_to_utf16(string_view s) {
   buffer_.push_back(0);
 }
 
+// BEGIN AXOM BUGFIX
 FMT_FUNC void format_system_error(detail::buffer<char>& out, int error_code,
                                   const char* message) noexcept {
   FMT_TRY {
     auto ec = std::error_code(error_code, std::generic_category());
-    detail::write(appender(out), std::system_error(ec, message).what());
+    detail::write(appender(out), axom_fmt_system_error(ec, message).what());
     return;
   }
   FMT_CATCH(...) {}
   format_error_code(out, error_code, message);
 }
+// END AXOM BUGFIX
 
 FMT_FUNC void report_system_error(int error_code,
                                   const char* message) noexcept {
